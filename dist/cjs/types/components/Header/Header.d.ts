import React, { ReactNode } from "react";
export declare const Header: ({ title, logo, alt, className, children, }: {
    title?: string;
    logo?: string;
    alt?: string;
    className?: string;
    children?: ReactNode;
}) => React.JSX.Element;

import React from "react";
import { Item, Title } from "./data";
export declare const ListCard: ({ bgcolor, title, items, className, }: {
    bgcolor?: string;
    title?: Title;
    items?: Item[];
    className?: string;
}) => React.JSX.Element;

import React from "react";
import { Item } from "./data";
export declare const ListNormal: ({ bgcolor, title, items, className, }: {
    bgcolor?: string;
    title?: string;
    items?: Item[];
    className?: string;
}) => React.JSX.Element;

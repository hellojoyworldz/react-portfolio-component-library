import React, { ElementType, ReactNode } from "react";
export declare const PortfolioContent: <T extends ElementType = "main">({ as, children, className, }: {
    as?: T;
    children: ReactNode;
    className?: string;
}) => React.JSX.Element;
